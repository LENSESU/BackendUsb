target-version = "py311"
line-length = 100
src = ["app"]

[lint]
select = ["E", "F", "I", "B"]
ignore = []

[lint.isort]
known-first-party = ["app"]

exclude = [
    ".venv",
    "venv",
    "env",
    "__pycache__",
]
